import { Pagination } from "@/types/common.type";

export const DEFAULT_LIMIT_PAGINATION: number = 10;

export const DEFAULT_PAGINATION_VALUE: Pagination = {
  limit: DEFAULT_LIMIT_PAGINATION,
  page: 1,
};
